# R environment snapshots — too large for git
*.RData
*.rda

# Saved model objects — regenerate with retrain scripts
*.rds

# API keys and secrets — never commit
.Renviron
.env
*.env

# CSV data files — raw data and backtest outputs
*.csv
# Exceptions: Python handoff data dumps
!train_inter.csv
!test_inter.csv
!odds_2020_2024.csv

# Plots / generated images — regenerate with calibration.R
*.png
*.pdf
*.typ

# R session artefacts
.Rhistory
.Rapp.history
.Rproj.user/
*.Rproj.user

# OS noise
.DS_Store
Thumbs.db
desktop.ini
